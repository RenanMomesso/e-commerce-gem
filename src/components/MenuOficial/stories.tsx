import { Story, Meta } from '@storybook/react'
import MenuOficial from '.'

export default {
  title: 'MenuOficial',
  component: MenuOficial
} as Meta

export const Default: Story = () => <MenuOficial />
