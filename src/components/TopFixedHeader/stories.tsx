import { Story, Meta } from '@storybook/react'
import TopFixedHeader from '.'

export default {
  title: 'TopFixedHeader',
  component: TopFixedHeader
} as Meta

export const Default: Story = () => <TopFixedHeader />
