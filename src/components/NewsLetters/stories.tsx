import { Story, Meta } from '@storybook/react'
import NewsLetters from '.'

export default {
  title: 'NewsLetters',
  component: NewsLetters
} as Meta

export const Default: Story = () => <NewsLetters />
